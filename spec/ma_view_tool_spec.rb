require "spec_helper"

describe MaViewTool do
  it "has a version number" do
    expect(MaViewTool::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
